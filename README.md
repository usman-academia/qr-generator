# QR Code Generator - Streamlit App

Generate QR codes locally, free and without expiry.

## Requirements

- Python 3.8+
- `pip` installed

## Install Dependencies

```bash
pip install -r requirements.txt
```

## Run the Application
```bash
streamlit run app.py
```
